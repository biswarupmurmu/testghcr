name: Build and Push Docker Image to GitHub Container Registry

on:
  push:
    branches:
      - main # Trigger workflow on push to the 'main' branch
  workflow_dispatch: # Allow manual trigger of the workflow

env:
  IMAGE_NAME: ${{ github.repository_owner }}/${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository code
      - name: Checkout code
        uses: actions/checkout@v3

      # Log in to GitHub Container Registry
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Build the Docker image
      - name: Build Docker Image
        run: |
          docker build -t $IMAGE_NAME:latest .

      # Push the Docker image to GitHub Container Registry
      - name: Push Docker Image
        run: |
          docker push $IMAGE_NAME:latest


# name: Docker Image CI

# on:
#   push:
#     branches: [ "main" ]
#   pull_request:
#     branches: [ "main" ]

# jobs:

#   build:

#     runs-on: ubuntu-latest

#     steps:
#     # # Log in to GitHub Container Registry
#     # - name: Log in to GitHub Container Registry
#     #   uses: docker/login-action@v2
#     #   with:
#     #     username: ${{ github.actor }}
#     #     password: ${{ secrets.GITHUB_TOKEN }}

#     # Build the Docker image
#     - name: Build Docker image
#       run: |
#         docker build -t ghcr.io/${{ github.repository }}/my-node-app:latest  --file Dockerfile .

#     # Push the Docker image to GHCR
#     - name: Push Docker image to GHCR
#       run: |
#         docker push ghcr.io/${{ github.repository }}/my-node-app:latest


    
#     # - uses: actions/checkout@v4
#     # - name: Build the Docker image
#     #   run: docker build . --file Dockerfile --tag helloworld:latest
#     # - name: Push Docker image to GHCR
#     #   run: docker push helloworld:latest
